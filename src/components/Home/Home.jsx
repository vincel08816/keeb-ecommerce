import React from "react";
import { HeroSlider, SponsorSlider } from "./components";

export default function Home() {
  return (
    <div>
      <HeroSlider />
      <SponsorSlider />

      {/* title text for hero image */}
      {/* Row of two images here */}
      {/* some sort of FAQ or guide thing? */}
      {/* add footer? hmmm... should I copy akko? */}
    </div>
  );
}
